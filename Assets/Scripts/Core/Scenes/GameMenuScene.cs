using UnityEngine;
using Core.Managers;

namespace Core.Scenes
{
    public class GameMenuScene : MonoBehaviour
    {
        private void Start()
        {
            Initialize();
        }

        private void Initialize()
        {
            Debug.Log("GameMenuScene initialized");
        }
    }
}
